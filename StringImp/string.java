package StringImp;

public class string {
    public static void main() {
        String str = "ABC";
        str.toLowerCase();
        str.toUpperCase();
        str.charAt(0);
        str.compareTo(str);
        str.contains("a");
        str.endsWith("b");
        str.indexOf(str);
        str.isBlank();
        str.isEmpty();
        str.lastIndexOf(str);
        str.length();
        str.replace("a", "b");
        str.startsWith(str);
    }

}
